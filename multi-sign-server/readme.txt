to setup:
bundle install

to run:
bundle exec multi-sign-server.rb

Note: before you run you will need to setup the configs
that are presently in the code.  we will later have an 
external config file.
only real config is to point at the path to the multisign.db
